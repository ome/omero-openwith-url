
from setuptools import setup, find_packages

setup(
    name='omero_openwith_url',
    version='0.0.1',
    description="An OMERO.web plugin for opening images via configured URLs",
    packages=find_packages(),
    keywords=['omero', 'openwith'],
)
